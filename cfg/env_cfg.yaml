env_name : Walker2DBulletEnv
algo : CMA-ES # choose algo
direction : forward # the direction of walking
num_keypoints : 6 # the number of key points
interpolate_interval : 100 # set the interpolate interval
n_iterations : 2000 # number of iterations
pop_size : 50 # the number of population
elite_frac : 0.1 # the percentage of elite; # of elite = elite_frac * pop_size
wandb : False # if use wandb
